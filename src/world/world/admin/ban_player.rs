use super::super::World;

impl World {
    pub fn ban_player(
        &mut self,
        victim_name: String,
        admin_name: String,
        duration: String,
        silent: bool,
    ) {
    }
}
